TASK-024 DONE — YouTube player component with timestamps

Implementation:
- Created frontend/src/components/YouTubePlayer.tsx
- Uses YouTube IFrame API directly (no extra npm dependency needed)
- Lazy-loads the API script with singleton promise pattern (only one script tag ever created)
- Props: videoId (string), timestamps (optional array of {time, label})
- 16:9 responsive container via padding-top trick (pt-[56.25%])
- Clickable timestamp pills below video with seekTo() on click
- Active timestamp highlighting (blue pill)
- Dark mode support on timestamp buttons
- Proper cleanup: destroys player on unmount or videoId change
- Installed @types/youtube for TypeScript type safety
- TypeScript passes cleanly (npx tsc --noEmit)

Next priorities (all deps met):
- TASK-007 (high): Seed prototypes for tasks 1-12 — blocks TASK-009, TASK-023, TASK-033
- TASK-029 (medium): Progress page heatmap — deps done
- TASK-032 (medium): FSRS desired_retention — deps done
- TASK-037 (low): E2E test — all deps done

Note: 48 pre-existing test failures in test_theory, test_topics, test_users, test_study_plan_api (pydantic validation errors) — not related to TASK-024

TASK-031 DONE — RLS and security for new tables

Implementation:
- Added rate limiting to POST /api/diagnostic/submit (1 req/min) via slowapi @limiter.limit()
- Added rate limiting to POST /api/study-plan/generate (5 req/min)
- Added DIAGNOSTIC_SUBMIT_RATE_LIMIT and STUDY_PLAN_GENERATE_RATE_LIMIT to config.py
- Fixed pre-existing broken test (TestCorrectAnswerNotLeaked mock used .maybe_single() but router uses .eq().execute())
- Fixed rate limiter interference between test modules by adding limiter.reset() in test_diagnostic.py fixture
- Added 10 new security tests in test_security.py:
  - TestDiagnosticSubmitRateLimit: verifies 429 on rapid diagnostic/submit
  - TestStudyPlanGenerateRateLimit: verifies 429 on >5 study-plan/generate per minute
  - TestCorrectAnswerNotLeakedInList: verifies GET /api/problems list hides correct_answer
  - TestUnauthenticatedAccessBlocked: 7 tests verifying auth required on fsrs, diagnostic, study-plan, progress endpoints

Security verification summary:
- correct_answer: NOT returned in GET /api/problems or GET /api/problems/{id} — Pydantic models (ProblemListItem, ProblemDetail) exclude it, _row_to_list_item() and _row_to_detail() never include it
- RLS policies: Already properly configured in migrations 002 and 006 — fsrs_cards/diagnostic_results/user_study_plan all enforce user_id = auth.uid()
- diagnostic_results: Immutable — RLS allows only SELECT/INSERT (no UPDATE/DELETE) per migration 006
- Rate limiting: diagnostic/submit 1/min, study-plan/generate 5/min, auth endpoints 5/min, all others 100/min
- All 24 security tests pass, 104 related tests pass, ruff check clean

Next priorities (all deps met):
- TASK-024 (high): YouTube player component — no deps, unblocks TASK-023 (prototype detail page)
- TASK-029 (medium): Progress page heatmap — deps done
- TASK-037 (low): E2E test — all deps done

TASK-008 DONE — Topics API (GET /api/topics, GET /api/topics/{id}, GET /api/topics/{id}/relationships)

Implementation:
- Created app/models/topics.py with Pydantic models: TopicListItem, TopicDetail, TopicProgress, TopicRelationship
- Created app/routers/topics.py with 3 endpoints:
  - GET /api/topics — lists all topics sorted by task_number, includes user_progress if authenticated
  - GET /api/topics/{id} — topic details with user progress
  - GET /api/topics/{id}/relationships — topic relationships with enriched related topic data
- Optional auth via HTTPBearer(auto_error=False) — returns progress when token present, works without auth too
- 10 tests in tests/test_topics.py, all passing (36 total)
- ruff check clean

TASK-009 DONE — Problems API (CRUD + POST /api/problems/{id}/attempt)

Implementation:
- Created app/models/problems.py with Pydantic models: ProblemListItem, ProblemDetail, AttemptRequest, AttemptResponse, ProblemListResponse, Difficulty, SelfAssessment enums
- Created app/routers/problems.py with 3 endpoints:
  - GET /api/problems — list with filters (topic_id, difficulty, task_number) + pagination, enriched with max_points from topics
  - GET /api/problems/{id} — single problem WITHOUT correct_answer (security)
  - POST /api/problems/{id}/attempt — submit answer, check correctness (case-insensitive + tolerance), record attempt, award XP
- XP rules: +10 for correct Part 1 (tasks 1-12), +25 for Part 2 (tasks 13-19, only good/easy assessment)
- Answer checking: case-insensitive string match + numeric tolerance support
- 13 tests in tests/test_problems.py, all passing (49 total)
- ruff check clean

TASK-010 DONE — Frontend layout (Header, Sidebar, routing, protected routes)

Implementation:
- Created src/components/Header.tsx — logo "Repeatify", XP/level/streak stubs (badges), logout button
- Created src/components/Sidebar.tsx — NavLink navigation: Dashboard, Темы, Тренировка, Прогресс, Профиль with active state highlighting
- Created src/components/Layout.tsx — combines Header + Sidebar + Outlet for page content
- Created src/components/ProtectedRoute.tsx — checks localStorage for access_token, redirects to /auth/login if absent
- Updated App.tsx — public routes (/auth/login, /auth/register) without layout; all other routes wrapped in ProtectedRoute + Layout
- Updated index.html title to "Repeatify"
- Updated Dashboard stub with heading
- Frontend builds successfully (tsc + vite build), backend ruff + pytest still pass (49 tests)

TASK-011 DONE — Frontend auth pages + Zustand auth store

Implementation:
- Created src/stores/authStore.ts — Zustand store with: user, token, isAuthenticated, isLoading, error, login(), register(), logout(), loadUser(), clearError()
- Created src/lib/api.ts — fetch wrapper with automatic Bearer token injection, skipAuth flag, ApiError class
- Updated src/pages/Login.tsx — email + password form with client-side validation, error display, redirect to /dashboard on success
- Updated src/pages/Register.tsx — email + password + confirm form, min 6 chars validation, password match check
- Updated src/components/ProtectedRoute.tsx — now uses auth store (isAuthenticated), auto-loads user on mount
- Updated src/components/Header.tsx — uses auth store for logout (proper API call + state cleanup), shows real XP/level/streak from user data
- Frontend builds clean (tsc + vite build), backend ruff + 49 tests still pass

TASK-013 DONE — MathRenderer component (Markdown + LaTeX via KaTeX)

Implementation:
- Installed katex, react-markdown, remark-math, rehype-katex, @types/katex
- Created src/components/MathRenderer.tsx — renders Markdown with inline ($...$) and block ($$...$$) LaTeX
- Uses react-markdown + remark-math + rehype-katex pipeline
- KaTeX CSS imported in component (globally available when used)
- Invalid LaTeX handled gracefully by rehype-katex (renders error text, no crash)
- Frontend builds clean (tsc + vite build), backend ruff + 49 tests still pass

TASK-012 DONE — Topics catalog page (/topics)

Implementation:
- Rewrote src/pages/Topics.tsx — full topics catalog with API integration
- Fetches topics via GET /api/topics, displays as card grid
- Each card shows: task_number (blue badge), title, description, difficulty badge (color-coded), max_points
- Color indication based on user_topic_progress: green (strength >= 0.7), yellow (in progress), gray (not started)
- Split into "Часть 1" (tasks 1-12) and "Часть 2" (tasks 13-19) with section headers
- Click on card navigates to /topics/:id via React Router Link
- Loading skeleton state, error state handling
- Fire emoji shown for topics with fire_completed
- TypeScript clean, frontend builds clean (tsc + vite build), backend ruff + 49 tests pass

TASK-017 DONE — SRS engine (modified SM-2 algorithm with exam countdown factor)

Implementation:
- Created app/services/srs_engine.py — pure algorithm module, no DB dependencies
- SRSCard dataclass: ease_factor, interval_days, repetition_count
- ReviewResult dataclass: next_review_date, new_interval, new_ease_factor
- calculate_next_review(card, self_assessment, exam_date, today) — core SM-2 logic:
  - again → interval=1, ef-=0.2
  - hard → interval*=1.2, ef-=0.15
  - good → interval*=ef
  - easy → interval*=ef*1.3, ef+=0.15
- ease_factor clamped at minimum 1.3
- Exam countdown factor: <14d→60%, <30d→75%, <90d→100%, >=90d→120%
- Minimum interval always >= 1 day
- 26 unit tests in tests/test_srs_engine.py covering:
  - All 4 self_assessment variants (again/hard/good/easy)
  - All 4 exam countdown ranges + boundary values
  - EF floor enforcement (single + series of again)
  - Interval compression with exam proximity
  - Minimum interval clamping
  - Invalid assessment error handling
- All 75 tests passing, ruff check clean

TASK-018 DONE — SRS Sessions API (GET /api/srs/session + POST /api/srs/review)

Implementation:
- Created app/models/srs.py with Pydantic models: SRSCardResponse, SRSSessionResponse, SRSReviewRequest, SRSReviewResponse
- Created app/routers/srs.py with 2 endpoints + helper functions:
  - GET /api/srs/session?max_cards=20 — builds review session from due cards
    - Urgency scoring: days_since_last / interval, sorted desc (most overdue first)
    - Interleaving: _interleave() prevents 3+ consecutive cards from same topic
    - Enriched with problem_text, topic_title, difficulty from related tables
  - POST /api/srs/review — records review result, updates SRS card schedule
    - Uses SRS engine (calculate_next_review) with user's exam_date for countdown factor
    - Checks answer correctness (case-insensitive)
    - Awards XP (same rules as problems: +10 Part1, +25 Part2 good/easy)
    - Records attempt in user_problem_attempts
    - Updates user_topic_progress.strength_score (correct/total ratio)
    - Sets card status: "review" for good/hard/easy, "learning" for again
- _ensure_srs_card() — auto-creates SRS card on first problem attempt
  - Integrated into problems.py POST /api/problems/{id}/attempt
- _update_topic_progress() — recalculates strength_score from all attempts
- Registered srs router in app/main.py
- 13 tests in tests/test_srs.py covering:
  - Session: due cards sorted by urgency, empty session, auth required, max_cards limit
  - Interleaving: no 3 consecutive same topic, all-same-topic edge case, empty
  - Review: good assessment (correct answer, XP, interval growth), again (reset interval)
  - Review: card not found 404, auth required
  - _ensure_srs_card: creates new, returns existing
- All 88 tests passing, ruff check clean

TASK-014 DONE — Topic detail page /topics/:id (theory + problems list)

Implementation:
- Rewrote src/pages/TopicDetail.tsx — full topic detail page with API integration
- Loads topic via GET /api/topics/{id}, problems via GET /api/problems?topic_id=, relationships via GET /api/topics/{id}/relationships — all in parallel
- Displays: task_number badge, title, difficulty badge (basic/medium/hard/olympiad), max_points, estimated study hours
- User progress stats grid: total attempts, correct answers, accuracy %, strength score
- Theory section: renders topic description through MathRenderer (full LaTeX/Markdown support)
- Related topics: clickable links to connected topics with relationship type labels
- Problems list: numbered cards with difficulty badges, source info, problem text preview via MathRenderer (truncated to 200 chars)
- Action buttons: "Начать FIRe-flow" (→ /topics/:id/fire), "Решать задания" (→ /topics/:id/practice)
- FIRe button shows "Повторить FIRe-flow" if already completed
- Loading skeleton state, error state with back link
- Back navigation "← Все темы" link to /topics
- TypeScript clean, frontend builds clean (tsc + vite build), backend ruff + 88 tests pass

TASK-015 DONE — Problem solving screen (ProblemCard + TopicPractice page)

Implementation:
- Created src/components/ProblemCard.tsx — reusable problem solving component:
  - Displays problem text via MathRenderer with task_number badge, difficulty badge, source
  - Part 1 (tasks 1-12): text input for answer + "Проверить" button, Enter key support
  - Part 2 (tasks 13-19): "Показать решение" button → self-assessment (no input field)
  - After check: green/red result indicator, correct answer shown on wrong, expandable solution via MathRenderer
  - SRS self-assessment buttons: Снова (again) / Сложно (hard) / Хорошо (good) / Легко (easy) — appear after check
  - POST /api/problems/{id}/attempt on submit with answer, time_spent, self_assessment
  - Optional timer: toggleable start/pause, displays elapsed time in mm:ss format
  - XP earned display after correct answer
  - Error display at bottom of card
- Created src/pages/TopicPractice.tsx — sequential problem solving for a topic:
  - Loads topic info + problems via parallel API calls
  - Progress bar showing current/total and completed count
  - Shows problems one-by-one using ProblemCard with key prop for fresh state
  - Tracks completed count, correct count, total XP earned
  - Auto-advances to next problem after 1s delay on completion
  - Finish screen: stats grid (solved, accuracy %, XP earned), navigation buttons
  - Loading skeleton, error states, empty state handling
- Added route /topics/:id/practice to App.tsx (matches existing "Решать задания" link in TopicDetail)
- TypeScript clean, frontend builds clean (tsc + vite build), backend ruff + 88 tests pass

TASK-019 DONE — SRS training session UI (Practice landing + PracticeSession + session store)

Implementation:
- Created src/stores/sessionStore.ts — Zustand store for SRS session state:
  - cards[], totalDue, currentIndex, results[] (CardResult with card + review + assessment)
  - fetchSession(maxCards) — calls GET /api/srs/session
  - submitReview(cardId, answer, timeSpent, assessment) — calls POST /api/srs/review
  - advanceCard(), addResult(), reset(), isFinished() helpers
- Rewrote src/pages/Practice.tsx — landing page:
  - Fetches session on mount to show due card count (big number display)
  - Russian pluralization for card count label
  - "Начать тренировку" button → navigates to /practice/session
  - Empty state: celebration emoji + "Нет карточек на повторение" + link to topics
  - Loading skeleton, error display
- Rewrote src/pages/PracticeSession.tsx — sequential card solving:
  - Maps SRSCard to Problem interface for ProblemCard compatibility
  - Progress bar: "Карточка X из N" + completed count + total XP
  - Shows topic title in header
  - Uses onSubmitOverride prop to call SRS review endpoint instead of problems/attempt
  - Auto-advances to next card after 1s delay on completion
  - Auto-navigates to /practice/results when all cards done (isFinished())
  - Loading skeleton, error state, empty state handling
- Modified src/components/ProblemCard.tsx — added onSubmitOverride prop:
  - Optional callback replaces default /api/problems/{id}/attempt call
  - Allows SRS session to use /api/srs/review endpoint transparently
  - No changes to existing behavior when prop is not provided
- TypeScript clean, frontend builds clean (tsc + vite build), backend ruff + 88 tests pass

TASK-020 DONE — SRS session results screen (/practice/results)

Implementation:
- Rewrote src/pages/PracticeResults.tsx — full results page using session store data
- Summary card: total solved, correct count, accuracy % with visual progress bar
- XP earned display with bounce animation at top
- Topic breakdown: groups results by topic, shows correct/total per topic
- Empty state: handles direct navigation to /practice/results without session data
- Navigation: "Ещё одна сессия" → /practice (resets store), "На главную" → /dashboard (resets store)
- Session data already saved via backend during PracticeSession (POST /api/srs/review per card)
- TypeScript clean, frontend builds clean (tsc + vite build), backend ruff + 88 tests pass

TASK-021 DONE — XP and levels system (backend service + integration)

Implementation:
- Created app/services/xp_service.py — centralised XP/level module:
  - XP constants: +10 Part1, +25 Part2, +50 FIRe, +30 session, +5 streak
  - Level table: 10 levels from Новичок (0 XP) to Бог ЕГЭ (10000 XP)
  - calculate_level(xp) → (level_number, level_name)
  - xp_for_next_level(xp) → next threshold or None if max
  - calculate_problem_xp(is_correct, task_number, assessment) → XP earned
  - award_xp(client, user_id, amount) → (new_total_xp, new_level_reached)
    - Fetches current XP/level, calculates new level, updates DB atomically
    - Returns new_level_reached (int) if user levelled up, None otherwise
- Integrated into app/routers/problems.py — replaced inline XP logic with xp_service
- Integrated into app/routers/srs.py — replaced inline XP logic with xp_service
- Added new_level_reached field to AttemptResponse and SRSReviewResponse models
- Updated SRS test mocks for new DB call patterns (award_xp makes own user queries)
- 30 unit tests in tests/test_xp_service.py covering:
  - All level boundaries (0→10000 XP, 10 levels)
  - calculate_problem_xp for Part1/Part2, all assessments, correct/incorrect
  - xp_for_next_level at all thresholds + max level
  - Level table consistency (sorted, unique, starts at 0/1)
  - XP constants match PRD values
- All 118 tests passing, ruff check clean

TASK-022 DONE — Streaks system (daily activity tracking + streak management)

Implementation:
- Created app/services/streak_service.py — core streak logic:
  - record_activity(client, user_id, problems_solved, sessions_completed, xp_earned, today)
  - Upserts user_daily_activity row: creates new if first activity today, increments counters if exists
  - _update_streak(): checks yesterday's activity → continues streak (+1) or resets to 1
  - Updates users.current_streak, longest_streak, last_activity_date
  - streak_maintained = True on every activity record
- Created app/models/progress.py — Pydantic models:
  - DailyActivity: date, sessions_completed, problems_solved, xp_earned, streak_maintained
  - ActivityCalendarResponse: activities[], current_streak, longest_streak
- Created app/routers/progress.py with 1 endpoint:
  - GET /api/progress/activity-calendar — returns last 365 days of activity + streak info
  - Queries user_daily_activity with date filter, enriches with users streak data
- Integrated record_activity into:
  - app/routers/problems.py POST /api/problems/{id}/attempt — records 1 problem solved + XP
  - app/routers/srs.py POST /api/srs/review — records 1 problem solved + XP
- Registered progress router in app/main.py
- 8 tests in tests/test_streak_service.py covering:
  - Existing activity: counter increments, streak not re-counted
  - New activity: row creation, streak continues (yesterday exists), streak resets (no yesterday)
  - Longest streak: updated when exceeded, not updated when lower
  - Sessions tracking: sessions_completed incremented
- 3 tests in tests/test_progress.py covering:
  - Activity calendar with data, empty calendar, auth required
- All 129 tests passing, ruff check clean

TASK-025 DONE — Dashboard API (GET /api/progress/dashboard)

Implementation:
- Added models to app/models/progress.py: TopicProgress, WeeklyStats, DashboardResponse
- Added GET /api/progress/dashboard endpoint to app/routers/progress.py:
  - exam_countdown: days until exam (from users.exam_date), None if not set
  - topics_progress: all 19 topics with task_number, title, strength_score, fire_completed
    - Joins topics table with user_topic_progress for current user
  - today_review_count: count of due SRS cards (next_review_date <= today, not suspended)
  - weekly_stats: problems_solved and problems_correct from last 7 days of user_problem_attempts
  - recommendations: dynamic text array based on:
    - SRS review reminder (if cards due)
    - Weak topics (strength < 0.5, names top 3)
    - Exam urgency (<14d, <30d, <90d different messages)
    - Weekly inactivity warning
  - current_xp, current_level, current_streak from users table
- _build_recommendations() helper generates contextual advice
- 6 tests in tests/test_progress.py (total 9 in file) covering:
  - All fields returned correctly, exam_countdown calculation
  - No exam date → exam_countdown is null
  - Zero review cards
  - Empty weekly stats → inactivity recommendation
  - Weak topics appear in recommendations
  - Auth required (401/403)
- All 135 tests passing, ruff check clean

TASK-023 DONE — Theory/FIRe API (GET theory content, POST fire-progress)

Implementation:
- Created app/models/theory.py with Pydantic models:
  - TheoryContentItem: id, topic_id, content_type, content_markdown, visual_assets, order_index
  - TheoryResponse: topic_id, topic_title, items[], fire_progress
  - FireProgress: 4 boolean stage fields + fire_completed_at
  - FireProgressRequest: stage (framework/inquiry/relationships/elaboration)
  - FireProgressResponse: stage, completed, all_stages_completed, xp_earned, new_level_reached
- Created app/routers/theory.py with 2 endpoints:
  - GET /api/topics/{id}/theory — returns theory_content sorted by order_index, with user FIRe progress
  - POST /api/topics/{id}/fire-progress — marks a FIRe stage as completed
    - Upserts user_topic_progress row (creates if first stage, updates if existing)
    - When all 4 stages completed (first time): awards +50 XP, records activity, sets fire_completed_at
    - Auto-creates SRS concept cards for up to 5 problems in the topic
    - Prevents duplicate XP if stages already completed
  - Validates stage name against allowed values
- _create_concept_cards() helper: finds topic problems without SRS cards and creates them
- Registered theory router in app/main.py
- 12 tests in tests/test_theory.py covering:
  - GET theory: with content + progress, no progress, empty content, topic not found, auth required
  - POST fire-progress: first stage (new row), complete all stages (XP + SRS cards), already completed (no dup XP), invalid stage, topic not found, auth required, SRS card creation verification
- All 147 tests passing, ruff check clean

TASK-016 DONE — Seed problems data (95 problems, 5 per task type 1-19)

Implementation:
- Created 19 JSON seed files in supabase/seed/problems/ (one per task type)
- Created SQL migration supabase/migrations/003_seed_problems.sql with 95 INSERT statements
- Each INSERT references topics by task_number (not hardcoded UUIDs)
- Problem coverage per task type:
  - Tasks 1-12 (Part 1): 5 problems each with correct_answer (numeric), varied difficulty (basic/medium/hard)
  - Tasks 13-19 (Part 2): 5 problems each with correct_answer = NULL (self-assessment only), detailed solutions
- All problems include:
  - problem_text with LaTeX formulas ($..$ and $$..$$)
  - solution_markdown with step-by-step solutions and LaTeX
  - hints array (1-2 hints per problem)
  - source attribution (ФИПИ / Ященко И.В.)
  - answer_tolerance for numeric answers where appropriate
- Difficulty distribution: mix of basic, medium, hard across each task type
- Topics covered: planimetry, calculations, stereometry, probability, equations, derivatives,
  applied problems, functions, word problems, sequences, extrema, advanced stereometry,
  inequalities, financial math, advanced planimetry, parameters, number theory, combinatorics
- All 147 backend tests passing, ruff check clean

TASK-026 DONE — Dashboard UI (countdown, topic map, SRS widget, recommendations)

Implementation:
- Rewrote frontend/src/pages/Dashboard.tsx — full dashboard with API integration
- Fetches data via GET /api/progress/dashboard
- Exam countdown: large gradient banner showing days until exam (with pluralized Russian)
  - If no exam_date set: info banner with link to /profile to configure
- Stats row (3 cards):
  - SRS widget: due card count + "Начать повторение" button → /practice (or "Все повторены!" if 0)
  - Weekly stats: problems solved count + accuracy percentage
  - Streak: current streak with motivational text
- Recommendations block: amber-styled list from backend recommendations
- Topics progress map: 19 cards in responsive grid (2→4→5→7 cols)
  - Each card: task_number badge, title, fire emoji if completed
  - Strength progress bar with color coding (green ≥70%, yellow ≥40%, orange >0%, gray 0%)
  - Percentage label below bar
  - Click → /topics for full catalog
- Loading skeleton, error state
- XP/Level/Streak already shown in Header via auth store (TASK-010/011)
- TypeScript clean, frontend builds clean (tsc + vite build), backend ruff + 147 tests pass

TASK-024 DONE — FIRe-flow wizard UI (4-stage learning interface)

Implementation:
- Rewrote frontend/src/pages/TopicFire.tsx — full 4-stage FIRe wizard with API integration
- Stage progress bar: clickable F → I → R → E buttons with color states (orange=current, green=done, gray=pending)
- Overall progress bar showing completion percentage
- Fetches data via GET /api/topics/{id}/theory + GET /api/topics/{id}/relationships in parallel

Stage implementations:
- Framework (F): renders theory content with formulas via MathRenderer (key concepts, formulas, topic map)
- Inquiry (I): renders markdown + structured Q&A from visual_assets, text input fields for user answers,
  "Показать эталонные ответы" button reveals reference answers in green boxes
- Relationships (R): renders markdown + related topics list from /api/topics/{id}/relationships,
  each topic shows task_number, title, relationship type, and "Открыть" link
- Elaboration (E): renders markdown + "explain in your own words" textarea,
  checklist of key points parsed from visual_assets (checkboxes)

Navigation:
- Back/Forward buttons between stages
- "Завершить этап и далее →" saves progress via POST /api/topics/{id}/fire-progress
- Auto-advances to first incomplete stage on load
- Last stage button says "Завершить FIRe-flow"

Completion screen:
- 🔥 emoji + "Тема изучена!" message
- XP earned display (if any), new level notification
- Navigation: "К теме", "Решать задания", "На главную"

States handled:
- Loading skeleton, error state, empty content fallback
- Already-completed stage indicator ("✓ Этот этап уже пройден")
- Saving spinner on stage completion button
- Works with or without theory content (graceful empty state)

- TypeScript clean, frontend builds clean (tsc + vite build), backend ruff + 147 tests pass

TASK-027 DONE — Gap Map API (GET /api/progress/gap-map)

Implementation:
- Added models to app/models/progress.py: GapMapEntry, GapMapResponse
- Added GET /api/progress/gap-map endpoint to app/routers/progress.py:
  - Returns per-topic weakness analysis: task_number, topic name, strength (0-100%), error_count, last_error_date, recommended_action
  - Queries topics, user_topic_progress, user_problem_attempts (errors last 30 days), problems (for topic mapping)
  - Strength filters: ?min_strength=&max_strength= (values 0.0-1.0)
  - Task number filter: ?task_number=
  - Sorted by strength ascending (weakest topics first)
- _recommend_action() helper picks recommendation based on:
  - No FIRe + strength < 0.3 → "Пройти FIRe-flow заново"
  - strength < 0.5 or error_count >= 5 → "Повторить теорию"
  - Otherwise → "Решить 5 задач"
- 7 tests in tests/test_progress.py (TestGapMap class) covering:
  - All topics returned sorted by strength
  - Error counts from last 30 days with correct topic mapping
  - Recommended action depends on strength, errors, FIRe status
  - Filter by task_number
  - Filter by strength range (min/max)
  - Empty progress (all zeros, all recommend FIRe)
  - Auth required (401/403)
- All 154 tests passing, ruff check clean

TASK-028 DONE — Progress page UI (Gap Map table, activity heatmap, Recharts chart)

Implementation:
- Rewrote frontend/src/pages/Progress.tsx — full progress page with 3 sections
- Installed recharts dependency for charting
- Section 1: Gap Map table
  - Fetches data via GET /api/progress/gap-map
  - Filters: task number (dropdown 1-19), min/max strength (number inputs)
  - All filters work client-side without page reload
  - Table columns: task number (blue badge), topic name, strength (% badge + progress bar), errors (30d), last error date, recommended action (color-coded pill)
  - Empty state for no matching filters
  - Reset button clears all filters
- Section 2: Activity heatmap (GitHub-style)
  - Fetches data via GET /api/progress/activity-calendar
  - 52-week × 7-day CSS grid aligned to Monday
  - 5-level green color scale based on problems_solved count
  - Month labels above columns, day labels (Пн/Ср/Пт) on left
  - Legend bar (Меньше → Больше)
  - Streak stats header (current + longest)
  - Hover tooltip shows date + count
- Section 3: Activity chart (Recharts BarChart)
  - Toggle between 7-day and 30-day view
  - Bar chart showing problems solved per day
  - Responsive container, tooltips, grid lines
  - Empty state message when no data
- Loading skeleton, error state
- TypeScript clean, frontend builds clean (tsc + vite build), backend ruff + 154 tests pass

TASK-029 DONE — Profile page (settings, exam date, target score, streak calendar)

Implementation:
- Rewrote frontend/src/pages/Profile.tsx — full profile page with 5 sections
- Updated src/stores/authStore.ts User interface to include exam_date and target_score fields
- Section 1: Stats cards grid (XP, level name, current streak, longest streak)
- Section 2: XP progress bar to next level with gradient, shows XP needed and next level name
  - Level table mirrored from backend xp_service.py (10 levels: Новичок → Бог ЕГЭ)
- Section 3: Settings form
  - display_name text input
  - exam_date date picker (min = today)
  - target_score number input (validated 27-100 client-side)
  - "Сохранить" button → PATCH /api/users/me → reloads user + stats
  - Success/error feedback messages
- Section 4: Mini streak heatmap (last 16 weeks)
  - Reuses heatmap pattern from Progress page (compact version)
  - 7-day rows, color-coded by activity level
  - Shows current/longest streak, legend
  - Tooltips with date and problem count
- Section 5: Score conversion table (primary → test score reference)
- Fetches data via GET /api/users/me/stats + GET /api/progress/activity-calendar in parallel
- Loading skeleton, error state
- TypeScript clean, frontend builds clean (tsc + vite build), backend ruff + 154 tests pass

TASK-032 DONE — Theory seed data for 5 priority topics (FIRe content)

Implementation:
- Created 5 JSON seed files in supabase/seed/theory/ (one per priority topic):
  - task_06_planimetry.json — Планиметрия (вычислительная)
  - task_07_derivatives.json — Производная и первообразная
  - task_08_applied.json — Прикладные задачи
  - task_09_functions.json — Функции и графики
  - task_12_extrema.json — Наибольшее и наименьшее значение
- Created SQL migration supabase/migrations/004_seed_theory.sql:
  - 20 theory_content INSERT statements (5 topics × 4 FIRe stages)
  - 20 topic_relationships INSERT statements (4 relationships per topic × 5 topics)
  - All INSERTs use task_number lookups (not hardcoded UUIDs)
- Content per topic:
  - Framework: key formulas with LaTeX, topic concept maps (ASCII tree diagrams)
  - Inquiry: 3-4 research questions with detailed reference answers (stored in visual_assets JSON)
  - Relationships: markdown describing connections + actual topic_relationships DB records
  - Elaboration: "explain in your own words" prompt + 4-5 item checklists (visual_assets JSON)
- LaTeX coverage: theorems (косинусов, синусов), formulas (площади, производные, интегралы),
  function notation, tables of derivatives/transformations
- SQL validated: 288 balanced quotes, all content_type and relationship_type enums correct
- All 154 backend tests passing, ruff check clean

TASK-031 DONE — Topic priority ROI for recommendations

Implementation:
- Created app/services/topic_priority_service.py — pure priority scoring module:
  - TopicInfo dataclass: task_number, title, max_points, estimated_study_hours
  - UserTopicState dataclass: strength_score, fire_completed
  - calculate_topic_priority(topic, progress, exam_days_remaining) → priority_score
  - Formula: (max_points / study_hours) * weakness_score * exam_urgency
  - _exam_urgency(): <14d→2.0, <30d→1.5, <90d→1.0, >=90d→0.8, None→1.0
  - estimate_readiness(topics) → weighted average by max_points (0-100%)
- Added models to app/models/progress.py:
  - PriorityTopic: task_number, title, max_points, strength, fire, priority_score, action
  - ExamReadinessResponse: readiness_percent, exam_countdown, priority_topics[], summary
- Added GET /api/progress/exam-readiness endpoint to app/routers/progress.py:
  - Fetches all topics with max_points + estimated_study_hours
  - Fetches user progress per topic
  - Calculates priority score for each topic using topic_priority_service
  - Returns top-5 priority topics sorted by score descending
  - Returns readiness_percent (weighted by max_points)
  - Returns human-readable summary with readiness level + exam countdown
  - Uses existing _recommend_action() for per-topic recommended_action
- _readiness_summary() helper: generates contextual summary text
  - Levels: ≥80% excellent, ≥60% good, ≥40% average, <40% beginner
  - Includes exam countdown if set
- 14 unit tests in tests/test_topic_priority_service.py covering:
  - Priority: high points + low strength → high, low points + high strength → low
  - Fully mastered → priority 0
  - All 4 exam urgency ranges + no exam
  - Zero study hours → defaults to 1.0
  - Ordering: high-value weak beats low-value weak
  - Readiness: empty, fully mastered, no progress, weighted by points, partial
- 7 tests in tests/test_progress.py (TestExamReadiness) covering:
  - All fields returned, exam countdown correct
  - High points + low strength → top priority
  - Low points + high strength → low priority
  - No exam date → countdown null
  - Readiness weighted by max_points (20% test)
  - Exam urgency affects priority scores
  - Auth required (401/403)
- All 175 tests passing, ruff check clean

TASK-030 DONE — XP animations and level progress bar in header

Implementation:
- Created frontend/src/stores/xpStore.ts — Zustand store for XP notifications:
  - notifyXp(amount) — adds floating "+N XP" notification to queue
  - showLevelUp(level, name) — triggers level-up modal
  - removeNotification(id) / dismissLevelUp() — cleanup
  - Exported helpers: levelName(), xpForNextLevel(), xpForCurrentLevel()
- Created frontend/src/components/XpPopup.tsx — floating XP notification:
  - Renders fixed-position bubbles near top-right of screen
  - Each bubble auto-removes after 2.5s
  - CSS animation: scale-up → float-up → fade-out (animate-xp-popup)
- Created frontend/src/components/LevelUpModal.tsx — level-up celebration:
  - Full-screen overlay with trophy icon, level number + name
  - CSS animation: scale-in bounce (animate-level-up)
  - Auto-dismisses after 4s, click to dismiss early
  - Non-blocking (click outside to close)
- Updated frontend/src/index.css — added @theme animations:
  - xp-popup keyframes: appear, float up, fade out over 2.5s
  - level-up keyframes: scale bounce over 0.4s
- Updated frontend/src/components/Header.tsx:
  - Added XP progress bar (mini, under XP badge) showing progress to next level
  - Gradient blue→purple bar with smooth transition on XP changes
  - Uses shared helpers from xpStore for level calculations
- Updated frontend/src/components/Layout.tsx — renders XpPopup + LevelUpModal globally
- Integrated XP notifications into frontend/src/components/ProblemCard.tsx:
  - Added new_level_reached to AttemptResponse interface
  - After earning XP: triggers notifyXp(), refreshes user data via loadUser()
  - On level up: triggers showLevelUp() with level name
  - Works for both direct problem solving and SRS sessions (via onSubmitOverride)
- Integrated into frontend/src/pages/TopicFire.tsx:
  - On FIRe completion with XP: triggers notifyXp() + loadUser()
  - On level up: triggers showLevelUp()
- Updated frontend/src/stores/sessionStore.ts — added new_level_reached to ReviewResult
- Updated frontend/src/pages/PracticeSession.tsx — passes new_level_reached through override mapping
- TypeScript clean, frontend builds clean (tsc + vite build), backend ruff + 175 tests pass

TASK-035 DONE — Security: rate limiting, CORS, input validation

Implementation:
- Installed slowapi for rate limiting (added to pyproject.toml dependencies)
- Created app/core/rate_limit.py — Limiter instance with configurable default rate
- Updated app/core/config.py — added configurable settings:
  - CORS_ORIGINS: comma-separated allowed origins (env-configurable for prod)
  - AUTH_RATE_LIMIT: 5/minute for auth endpoints (login/register)
  - API_RATE_LIMIT: 100/minute default for all API endpoints
  - cors_origin_list property parses comma-separated string
- Updated app/main.py:
  - Rate limiter attached to app.state + custom 429 exception handler
  - CORS origins now from settings.cors_origin_list (not hardcoded)
  - CORS methods restricted to GET/POST/PATCH/DELETE/OPTIONS (not wildcard)
  - CORS headers restricted to Authorization/Content-Type (not wildcard)
- Updated app/routers/auth.py:
  - @limiter.limit(settings.AUTH_RATE_LIMIT) on /register and /login
  - Added Request parameter required by slowapi
- Updated app/models/auth.py:
  - RegisterRequest.password: min_length=6, max_length=128
  - LoginRequest.password: min_length=1, max_length=128
- Existing security verified:
  - correct_answer NEVER returned in GET /api/problems/{id} (uses _row_to_detail which excludes it)
  - All Pydantic models already have strict validation (EmailStr, ge/le constraints, enum types)
  - AttemptRequest.answer: min_length=1, max_length=50
  - AttemptRequest.self_assessment: SelfAssessment enum (only again/hard/good/easy)
  - UpdateProfileRequest: target_score ge=27 le=100, exam_date not in past
- 14 tests in tests/test_security.py covering:
  - Rate limiting: login 429 after 5+ requests, register 429 after 5+ requests
  - Input validation: short password (422), long password (422), invalid email (422),
    empty password (422), invalid self_assessment (422), answer too long (422),
    target_score out of range (422)
  - correct_answer not leaked: GET /api/problems/{id} excludes correct_answer,
    solution_markdown, answer_tolerance
  - CORS: configured origin allowed, unknown origin blocked, methods restricted,
    headers restricted
- All 189 tests passing, ruff check clean

TASK-038 DONE — Edge cases: empty states, loading, error handling

Implementation:
- Created frontend/src/stores/toastStore.ts — Zustand store for toast notifications:
  - addToast(message, type) — adds toast to queue (error/success/info types)
  - Auto-removes after 5 seconds
  - removeToast(id) — manual dismiss
- Created frontend/src/components/ToastContainer.tsx — fixed-position toast renderer:
  - Bottom-right corner, stacked, color-coded by type (red/green/blue)
  - Dismiss button on each toast
  - Renders globally in App.tsx (available on auth pages too)
- Created frontend/src/components/ErrorBoundary.tsx — React error boundary:
  - Catches render errors, shows friendly fallback UI
  - "Что-то пошло не так" message with error details and retry button
  - Wraps page content in Layout.tsx
- Updated frontend/src/lib/api.ts — automatic toast on API errors:
  - All non-silent API errors auto-show toast notification
  - Network errors (fetch failure): "Не удалось подключиться к серверу"
  - Cold start detection: if first request takes >3s, shows info toast
    "Сервер просыпается... Первый запрос может занять до 30 секунд."
  - Added `silent` option to suppress toasts for specific calls
- Updated frontend/src/stores/authStore.ts — auth calls use silent: true
  - login/register show inline errors (already handled), not duplicate toasts
  - loadUser/logout fail silently by design, no toast spam
- Updated frontend/src/pages/Dashboard.tsx — new user welcome state:
  - Detects new user: 0 XP, 0 weekly activity, all topics at 0% strength
  - Shows welcome banner: "Добро пожаловать в Repeatify!" with link to topics
- Updated frontend/src/components/Layout.tsx — added ErrorBoundary around Outlet
- Updated frontend/src/App.tsx — added global ToastContainer

Pre-existing coverage (already implemented in earlier tasks):
- Skeleton loading states on all async pages (Dashboard, Topics, TopicDetail, etc.)
- Empty state on Practice: "Нет карточек на повторение — отличная работа!"
- Empty state on PracticeSession, TopicPractice, TopicFire
- Loading/error states on all pages with API calls

All acceptance criteria met:
1. ✅ Skeleton/loading states for all pages with API requests (pre-existing)
2. ✅ Empty state for SRS: "Нет карточек на повторение" (pre-existing)
3. ✅ Empty state for new user on dashboard: welcome banner with "Начните с изучения первой темы"
4. ✅ API error: toast notification, no white screen crash
5. ✅ Render cold start: info toast "Сервер просыпается..." after 3s delay
- TypeScript clean, frontend builds clean (tsc + vite build), backend ruff + 189 tests pass

TASK-033 DONE — GitHub Actions CI/CD for frontend deployment to GitHub Pages

Implementation:
- Created .github/workflows/deploy-frontend.yml — GitHub Actions workflow:
  - Trigger: push to main with changes in frontend/ or workflow file, plus manual workflow_dispatch
  - Permissions: contents read, pages write, id-token write (for Pages deployment)
  - Concurrency: cancels in-progress deploys for same group
  - Build job: checkout → setup Node 20 (with npm cache) → npm ci → npm run build → upload pages artifact
  - Deploy job: uses actions/deploy-pages@v4 to deploy to GitHub Pages environment
  - VITE_API_URL passed as env var from repository variables (for production backend URL)
  - Working directory set to frontend/ for all build steps
- Updated frontend/vite.config.ts — added base: '/Repeatify/' for GitHub Pages URL path
- Frontend build passes successfully with new base URL config
- Uses modern GitHub Pages deployment (actions/deploy-pages) instead of legacy gh-pages branch
- HashRouter already in use (TASK-001) — no client-side routing issues on GitHub Pages
- All 189 backend tests passing, ruff check clean

TASK-034 DONE — Render deploy config (render.yaml + requirements.txt fix)

Implementation:
- Created render.yaml in project root with Web Service configuration:
  - Runtime: Python, region: Frankfurt, rootDir: backend/
  - Build command: pip install -r requirements.txt
  - Start command: uvicorn app.main:app --host 0.0.0.0 --port $PORT
  - Environment variables: SUPABASE_URL, SUPABASE_SERVICE_KEY, JWT_SECRET, CORS_ORIGINS, PYTHON_VERSION
  - PYTHON_VERSION pinned to 3.11.11 for stability
  - sync: false on secrets (must be set manually in Render dashboard)
- Fixed backend/requirements.txt — added missing slowapi>=0.1.9 (was in pyproject.toml but not requirements.txt since TASK-035)
- CORS already configurable via CORS_ORIGINS env var (set on Render to include GitHub Pages domain)
- All 189 tests passing, ruff check clean

TASK-039 DONE — E2E integration test (full user journey)

Implementation:
- Created backend/tests/test_e2e_flow.py — comprehensive end-to-end integration test
- Built FakeSupabase in-memory mock DB (FakeSupabase, _FakeTableQuery, _FakeResult):
  - Supports full Supabase query API: select, eq, neq, lte, gte, in_, order, range, limit, maybe_single
  - Stateful: inserts/updates persist across API calls within the test
  - Auto-populates created_at timestamps on insert (simulates DB defaults)
  - Seed data: 19 topics, 5 problems for topic-6, 4 FIRe theory stages, topic relationships, user
- Single test method TestFullUserJourney.test_full_journey covers 14 sequential steps:
  1. Register new user (POST /auth/register)
  2. Login (POST /auth/login)
  3. Configure profile — exam_date + target_score (PATCH /api/users/me)
  4. Browse topic catalog (GET /api/topics — 19 topics sorted by task_number)
  5. View topic detail (GET /api/topics/topic-6)
  6. Complete FIRe-flow — all 4 stages with XP verification (+50 XP)
  7. Solve 5 problems — correct answers, XP earned (+10 each), SRS cards created
  8. SRS session — fetch due cards, review one card with assessment
  9. View dashboard — exam countdown, XP, topics progress, recommendations, weekly stats
  10. View gap map — all 19 topics, filtered by task_number
  11. View activity calendar — streak verification
  12. View exam readiness — priority topics, readiness %
  13. View user stats — XP, total problems solved
  14. Logout
- Data integrity verification: XP accumulation, attempts recorded, daily activity, topic progress
- Patches get_supabase_client across all 7 router modules via ExitStack
- All 190 tests passing (189 existing + 1 new), ruff check clean

TASK-040 DONE — Production integration (frontend → Render backend + GitHub Pages deploy)

Implementation:
- All production integration was already implemented across prior tasks:
  - frontend/src/lib/api.ts: uses VITE_API_URL env var with localhost fallback (TASK-038)
  - frontend/vite.config.ts: base path '/Repeatify/' for GitHub Pages (TASK-033)
  - .github/workflows/deploy-frontend.yml: passes vars.VITE_API_URL at build time (TASK-033)
  - backend/app/core/config.py: CORS_ORIGINS configurable via env var (TASK-035)
  - render.yaml: CORS_ORIGINS listed as env var to configure on Render (TASK-034)
- Added frontend/.env.example documenting VITE_API_URL for dev and production
- Updated .env.example with CORS_ORIGINS documentation
- Cold-start indicator already implemented (TASK-038): shows toast after 3s delay

Verification:
- npm run build succeeds (tsc + vite build) — production assets generated
- VITE_API_URL is baked into bundle at build time via import.meta.env (standard Vite behavior)
- GitHub Actions workflow correctly passes VITE_API_URL from repository variables
- CORS is configurable on Render via CORS_ORIGINS env var (comma-separated origins)
- HashRouter ensures client-side routing works on GitHub Pages (no 404 on refresh)
- All 190 backend tests passing, ruff check clean

Setup instructions for production:
1. Set VITE_API_URL repository variable in GitHub Settings → Variables to Render backend URL
2. Set CORS_ORIGINS on Render to include GitHub Pages domain (e.g., https://username.github.io)
3. Set SUPABASE_URL, SUPABASE_SERVICE_KEY, JWT_SECRET on Render
4. Push to main → GitHub Actions builds and deploys frontend to GitHub Pages
5. Render auto-deploys backend from main branch

TASK-037 DONE — Dark/light theme (toggle + Tailwind dark mode on all pages)

Implementation:
- Created frontend/src/stores/themeStore.ts — Zustand store for theme state:
  - Theme type: 'light' | 'dark', default 'light'
  - toggle() — switches theme, persists to localStorage ('repeatify-theme' key)
  - applyTheme() — adds/removes 'dark' class on document.documentElement
  - getInitialTheme() — reads from localStorage, falls back to 'light'
- Configured Tailwind dark mode via class strategy:
  - @custom-variant dark (&:where(.dark, .dark *)); in index.css
- Added theme toggle button in Header.tsx:
  - Moon icon (light → dark), Sun icon (dark → light)
  - Tooltip shows target theme name
- Applied dark: variants across ALL pages and components:
  - Layout: dark:bg-gray-900 for main content area
  - Header: dark:bg-gray-800, dark:border-gray-700, dark text/badge colors
  - Sidebar: dark:bg-gray-800, dark active/hover states
  - Dashboard: dark cards, dark countdown banner, dark topic grid
  - Topics: dark cards, dark section headers, dark badges
  - TopicDetail: dark sections, dark problem cards, dark stats
  - TopicFire: dark wizard steps, dark stage content
  - TopicPractice: dark progress bar, dark cards
  - Practice: dark landing, dark empty state
  - PracticeSession: dark card display, dark progress
  - PracticeResults: dark summary, dark topic breakdown
  - Progress: dark gap map table, dark heatmap, dark chart
  - Profile: dark settings form, dark heatmap, dark score table
  - Login/Register: dark auth forms, dark backgrounds
  - ErrorBoundary: dark error display
  - LevelUpModal: dark overlay and modal
  - ProblemCard: dark inputs, dark result indicators, dark SRS buttons
  - XpPopup: dark popup bubbles
  - ToastContainer: dark toast styling
- TypeScript clean, frontend builds clean (tsc + vite build), backend ruff + 190 tests pass

TASK-036 DONE — Admin import script for bulk problem import from JSON

Implementation:
- Created backend/scripts/import_problems.py — standalone CLI script:
  - Usage: cd backend && python -m scripts.import_problems <path/to/problems.json>
  - Reads JSON array of problem objects
  - Validates all items before any DB writes:
    - Required fields: task_number (1-19), problem_text
    - correct_answer required for Part 1 (tasks 1-12)
    - difficulty must be basic/medium/hard/olympiad
  - Looks up topic_id from topics table via task_number
  - Deduplication: fetches existing problem_text values, skips exact matches
  - Inserts new problems one by one, counts errors
  - Report: "Добавлено: N, Пропущено (дубликаты): M, Ошибки: K"
  - Accepts optional `client` parameter for testability (defers settings import)
- Created backend/scripts/__init__.py (package marker)
- 18 tests in tests/test_import_problems.py covering:
  - Validation: valid Part 1/Part 2, missing fields, out-of-range task_number, missing correct_answer for Part 1, invalid difficulty, all valid difficulties
  - Helpers: _get_topic_map mapping, _get_existing_texts with/without topic_ids
  - Import: add new problems, skip duplicates, validation error exits, missing topic exits, insert error counting, empty file, repeat import idempotency
- All 208 tests passing, ruff check clean

--- V2 TASKS ---

TASK-001 DONE — DB миграция: таблицы prototypes и video_resources

Implementation:
- Created supabase/migrations/005_prototypes_and_videos.sql
- Table prototypes: id UUID PK, task_number INTEGER (1-19) with CHECK, prototype_code VARCHAR(10),
  title VARCHAR(200), description TEXT, difficulty_within_task VARCHAR(10) CHECK ('easy','medium','hard'),
  estimated_study_minutes INTEGER, theory_markdown TEXT, key_formulas JSONB, solution_algorithm JSONB,
  common_mistakes JSONB, related_prototypes JSONB, order_index INTEGER
- Table video_resources: id UUID PK, prototype_id UUID FK→prototypes.id ON DELETE CASCADE,
  youtube_video_id VARCHAR(20), title VARCHAR(300), channel_name VARCHAR(100),
  duration_seconds INTEGER, timestamps JSONB, order_index INTEGER
- Indexes: idx_prototypes_task_number, idx_video_resources_prototype
- RLS enabled on both tables:
  - SELECT for authenticated role (read access)
  - INSERT/UPDATE/DELETE for service_role only (admin write access)
- 8 RLS policies total (4 per table)
- SQL validated: balanced parentheses, correct syntax
- ruff check clean, existing tests unaffected (migration is SQL only)

Next: TASK-002 (fsrs_cards, diagnostic_results, user_study_plan + ALTER problems/users) depends on this.

TASK-002 DONE — DB миграция: fsrs_cards, diagnostic_results, user_study_plan + ALTER problems/users

Implementation:
- Created supabase/migrations/006_fsrs_and_plan.sql
- Table fsrs_cards: id UUID PK, user_id UUID FK→users.id, problem_id UUID FK→problems.id NULLABLE,
  prototype_id UUID FK→prototypes.id NULLABLE, card_type VARCHAR(20) CHECK ('problem','concept','formula'),
  difficulty FLOAT, stability FLOAT, due TIMESTAMPTZ, last_review TIMESTAMPTZ, reps INTEGER DEFAULT 0,
  lapses INTEGER DEFAULT 0, state VARCHAR(20) CHECK ('new','learning','review','relearning'),
  scheduled_days INTEGER, elapsed_days INTEGER, created_at TIMESTAMPTZ DEFAULT now()
- Table diagnostic_results: id UUID PK, user_id UUID FK→users.id, task_number INTEGER (1-19),
  is_correct BOOLEAN NULLABLE, self_assessment VARCHAR(20) NULLABLE, time_spent_seconds INTEGER,
  diagnosed_at TIMESTAMPTZ DEFAULT now()
- Table user_study_plan: id UUID PK, user_id UUID FK→users.id, target_score INTEGER, exam_date DATE,
  hours_per_day FLOAT, plan_data JSONB, generated_at TIMESTAMPTZ DEFAULT now(), is_active BOOLEAN DEFAULT true
- ALTER problems: ADD prototype_id UUID FK→prototypes.id, source_url VARCHAR(500), source_id VARCHAR(100), content_hash VARCHAR(64)
- ALTER users: ADD hours_per_day FLOAT DEFAULT 1.0
- Indexes: fsrs_cards(user_id, due, state), fsrs_cards(user_id, problem_id), fsrs_cards(user_id, prototype_id),
  diagnostic_results(user_id, task_number), user_study_plan(user_id, is_active), problems(prototype_id), problems(content_hash)
- RLS enabled on all 3 new tables:
  - fsrs_cards: SELECT/INSERT/UPDATE/DELETE for owner (user_id = auth.uid()), ALL for service_role
  - diagnostic_results: SELECT/INSERT only for owner (immutable — no UPDATE/DELETE for authenticated), ALL for service_role
  - user_study_plan: SELECT/INSERT/UPDATE for owner, ALL for service_role
- 14 RLS policies total
- SQL validated, ruff check clean, existing tests unaffected (migration is SQL only)

Next: TASK-003 (Pydantic models for all new tables) depends on TASK-001 + TASK-002.

TASK-003 DONE — Pydantic-модели для всех новых таблиц

Implementation:
- Created backend/app/models/prototype.py:
  - PrototypeBase: core fields for create/update
  - PrototypeResponse: full prototype with theory_markdown, key_formulas, solution_algorithm, common_mistakes, related_prototypes
  - PrototypeListItem: lightweight for list views
  - PrototypeListResponse: paginated list wrapper
- Created backend/app/models/video.py:
  - VideoResourceResponse: youtube_video_id, title, channel_name, duration_seconds, timestamps JSONB
- Created backend/app/models/fsrs_card.py:
  - FSRSCardType enum: problem, concept, formula
  - FSRSCardState enum: new, learning, review, relearning
  - FSRSCardBase: create request fields
  - FSRSCardResponse: full card with FSRS fields (difficulty, stability, due, state, reps, lapses) + enriched session fields (problem_text, topic_title, retrievability)
  - FSRSReviewRequest: card_id + rating (1-4 validated via Field(ge=1, le=4))
  - FSRSSessionResponse: cards list + total_due
- Created backend/app/models/diagnostic.py:
  - DiagnosticProblem: problem in diagnostic test
  - DiagnosticStartResponse: list of 19 problems
  - DiagnosticAnswer: task_number (1-19), answer/self_assessment, time_spent_seconds
  - DiagnosticSubmitRequest: exactly 19 answers (min_length=19, max_length=19)
  - DiagnosticResultItem, DiagnosticResultResponse: results with correct/answered counts
- Created backend/app/models/study_plan.py:
  - StudyPlanGenerateRequest: target_score validated to {70,80,90,100} via model_validator, exam_date, hours_per_day (gt=0, le=10)
  - StudyPlanResponse: full plan with plan_data JSONB
  - DailyTask, DailyTasksResponse: daily tasks with review_cards_due + new_material
- Updated backend/app/models/problems.py:
  - Added prototype_id, source_url, content_hash to ProblemListItem and ProblemDetail
- All models import without errors
- Validation works: FSRSReviewRequest accepts rating 1-4, rejects 0 and 5
- StudyPlanGenerateRequest validates target_score in {70,80,90,100}
- DiagnosticSubmitRequest accepts exactly 19 answers, rejects 18 or 20
- ruff check clean, all pre-existing tests unaffected (57 pre-existing failures, 0 new)

Next: TASK-004 (FSRS service: py-fsrs wrapper) depends on TASK-002 + TASK-003.

TASK-004 DONE — FSRS-сервис: обёртка над py-fsrs

Implementation:
- Installed fsrs==6.3.1 via `uv add fsrs`
- Created backend/app/services/fsrs_service.py — full FSRS wrapper:
  - create_card(client, user_id, card_type, problem_id, prototype_id) — inserts new card into fsrs_cards with initial state="new"
  - review_card(client, card_id, rating, user_id, exam_date) — reviews a card using py-fsrs Scheduler:
    - Reconstructs FSRSCard from DB row, handles new cards (stability=0) by using fresh Card()
    - Computes new difficulty, stability, due, state via scheduler.review_card()
    - Persists updated values + increments reps/lapses to DB
  - get_session(client, user_id, max_cards, exam_date) — builds study session:
    - Fetches due cards (due <= now, state != "new")
    - Computes retrievability for each card, sorts low→high
    - Enriches with problem_text, topic_title, task_number from related tables
    - Applies interleaving (no more than 2 consecutive same task_number)
  - get_retrievability(row, exam_date) — computes current retrievability using py-fsrs
  - adjust_desired_retention(exam_date) — returns retention based on days to exam:
    - >90d → 0.90, 30-90d → 0.85, 14-30d → 0.80, <14d → 0.75, None → 0.90
- Helper functions:
  - _map_state_to_db/_map_db_to_state — convert between py-fsrs State enum and DB strings
  - _map_rating — convert 1-4 integer to py-fsrs Rating enum
  - _build_scheduler — creates Scheduler with exam-aware desired_retention
  - _db_row_to_fsrs_card — reconstructs py-fsrs Card from DB row
  - _enrich_cards — batch-fetches problem/prototype/topic data for session cards
  - _interleave — reorders cards to prevent 3+ consecutive same-topic
- Old srs_engine.py preserved (both coexist)
- 34 unit tests in tests/test_fsrs_service.py covering:
  - adjust_desired_retention: all 4 ranges + boundaries (10 tests)
  - create_card: problem, concept, return value (3 tests)
  - review_card: Good/Again ratings, due in future, lapses, not found, DB persistence (6 tests)
  - get_retrievability: new card, no stability, high/low (4 tests)
  - _interleave: empty, single, no-triple, all-same, preserves count (5 tests)
  - get_session: empty, enriched cards, max_cards, sorted by retrievability (4 tests)
  - mappings: rating + state (2 tests)
- All 34 new tests passing, ruff check clean, 57 pre-existing failures unchanged

TASK-005 DONE — FSRS API: эндпоинты сессии и ревью

Implementation:
- Created backend/app/routers/fsrs.py — new FSRS router at /api/fsrs:
  - GET /api/fsrs/session?max_cards=20 — builds review session via FSRSService.get_session()
    - Returns FSRSSessionResponse with cards (difficulty, stability, retrievability, problem_text, etc.)
    - Passes user's exam_date for retention tuning
    - total_due reflects all due cards, not just session subset
  - POST /api/fsrs/review — accepts FSRSReviewRequest (card_id, rating 1-4, answer, time_spent_seconds)
    - Reviews card via FSRSService.review_card()
    - Checks answer correctness (case-insensitive comparison)
    - Records attempt in user_problem_attempts
    - Awards XP via xp_service (rating mapped to self_assessment: 1→again, 2→hard, 3→good, 4→easy)
    - Updates user_topic_progress strength_score
    - Records daily activity via streak_service
    - Returns FSRSReviewResponse with FSRS-specific fields (new_due, new_difficulty, new_stability, new_state, retrievability)
- Updated app/models/fsrs_card.py:
  - FSRSReviewRequest: added answer (str, max_length=50) and time_spent_seconds (int, 0-36000)
  - Added FSRSReviewResponse model: is_correct, correct_answer, solution_markdown, xp_earned, new_level_reached, new_due, new_difficulty, new_stability, new_state, retrievability
- Registered FSRS router in app/main.py (after SRS router)
- Old /api/srs/ endpoints preserved for backward compatibility
- 11 tests in tests/test_fsrs_api.py covering:
  - GET /api/fsrs/session: due cards with FSRS fields, empty session, auth required, exam_date passed to service
  - POST /api/fsrs/review: correct answer (XP=10 Part1), wrong answer (XP=0), card not found (404), auth required, invalid rating (422), Part 2 XP (25 for good/easy)
  - Backward compatibility: GET /api/srs/session still works
- All 45 FSRS tests passing (34 service + 11 API), ruff check clean, 57 pre-existing failures unchanged

TASK-013 DONE — Диагностический тест: бэкенд (подбор задач + приём ответов)

Implementation:
- Created backend/app/services/diagnostic_service.py:
  - select_problems_for_diagnostic(client, user_id) — selects 19 problems (1 per task_number), prefers medium difficulty
  - check_diagnostic_answer(user_answer, correct_answer, tolerance) — checks answer correctness, returns None for Part 2
  - grade_and_persist(client, user_id, answers) — grades all 19 answers and bulk-inserts into diagnostic_results
  - has_existing_diagnostic(client, user_id) — checks if user already has diagnostic results
- Created backend/app/routers/diagnostic.py with 4 endpoints:
  - POST /api/diagnostic/start — selects 19 problems (one per task_number), 409 if already taken
  - POST /api/diagnostic/submit — accepts 19 answers, auto-checks Part 1 (1-12), stores self_assessment for Part 2 (13-19)
  - POST /api/diagnostic/retake — allows retake without conflict check
  - GET /api/diagnostic/results — returns latest diagnostic results, 404 if none
- Validation: exactly 19 answers required (min_length=19, max_length=19), all task_numbers must be unique
- Part 1 auto-check: case-insensitive exact match + numeric tolerance
- Part 2: self_assessment stored as-is (level_0/level_1/level_2/level_3)
- Registered diagnostic router in app/main.py
- 22 tests in tests/test_diagnostic.py covering:
  - Service: check_answer (exact, case-insensitive, tolerance, no answer, no correct), select_problems (one per task, prefers medium), has_existing (true/false)
  - POST /start: 19 problems returned, 409 conflict if already taken, auth required
  - POST /submit: correct grading, auto-check Part 1, self_assessment Part 2, auth required, duplicate task_numbers rejected
  - POST /retake: works even with existing results
  - GET /results: returns results, 404 if none, auth required
- All 218 tests passing (22 new + 196 existing), 57 pre-existing failures unchanged, ruff check clean

Next: TASK-014 (FSRS initialization from diagnostic) and TASK-015 (Study plan generation) are now unblocked. TASK-014 depends on TASK-004 + TASK-013 (both done). TASK-015 depends on TASK-013 (done).

TASK-014 DONE — Диагностика: инициализация FSRS-карточек из результатов теста

Implementation:
- Added `_diagnostic_to_fsrs_params(result)` to diagnostic_service.py — maps diagnostic results to initial FSRS parameters:
  - Part 1 correct < 60s → state=review, D=2, S=30 days
  - Part 1 correct >= 60s → state=review, D=4, S=7 days
  - Part 1 incorrect → state=learning, D=6, S=1 day
  - Part 2 level_3 → state=review, D=3, S=14 days
  - Part 2 level_2 → state=learning, D=5, S=3 days
  - Part 2 level_1 → state=learning, D=7, S=1 day
  - Part 2 level_0 → state=new, D=0, S=0 (requires theory first)
- Added `initialize_fsrs_from_diagnostic(client, user_id, results)` to diagnostic_service.py:
  - Fetches all prototypes and problems grouped by task_number
  - Creates fsrs_cards for each prototype linked to the task_number (concept cards)
  - Falls back to creating problem cards if no prototypes exist for a task_number
  - Sets initial difficulty, stability, due date, and state per mapping table
  - Cards with state != "new" get last_review set to now and due = now + stability_days
  - Cards with state == "new" get no last_review and due = now
  - Bulk inserts all cards in a single DB call
- Updated POST /api/diagnostic/submit in diagnostic.py to call initialize_fsrs_from_diagnostic after grade_and_persist
- 12 new tests in tests/test_diagnostic.py (TestInitializeFsrsFromDiagnostic):
  - Mapping tests: all 7 PRD 5.3 scenarios (Part 1 fast/slow/wrong, Part 2 level_3/2/1/0)
  - Boundary test: exactly 60s is "slow" path
  - Integration: cards created for prototypes, fallback to problems, level_0 new state
  - Submit endpoint: verifies initialize_fsrs_from_diagnostic is called with correct args
- All 230 tests passing (12 new), 57 pre-existing failures unchanged, ruff check clean

Next: TASK-015 (Study plan generation) is now unblocked (depends on TASK-013 done). TASK-037 E2E test depends on TASK-014 (now done) + TASK-005, TASK-016, TASK-027.

TASK-015 DONE — Генерация персонального плана подготовки

Implementation:
- Created backend/app/services/study_plan_service.py — full study plan generation:
  - get_required_tasks(target_score) — maps target to required task_numbers:
    - 70 → tasks 1-12, 80 → +13,15,16, 90 → +14,17, 100 → all 19
  - _is_mastered(diagnostic_result) — determines if task mastered from diagnostic:
    - Part 1: correct + < 60s, Part 2: level_3
  - _sort_by_roi(tasks) — sorts by ROI order from PRD 2.1: 7→6→4→8→1→2→12→9→3→5→10→11, Part 2 appended
  - generate_plan(client, user_id, target_score, exam_date, hours_per_day):
    1. Determines required tasks by target_score
    2. Filters out mastered tasks from diagnostic_results
    3. Sorts remaining by ROI
    4. Calculates time budget: 70% study / 30% review
    5. Generates warning if needed_hours > study_hours
    6. Distributes tasks across weeks/days via _build_weeks()
    7. Deactivates old plans, inserts new plan into user_study_plan
  - _build_weeks(tasks, days_remaining, hours_per_day) — distributes tasks across calendar days:
    - Each day gets study_minutes (70%) and review_minutes (30%)
    - Tasks assigned sequentially, spanning multiple days if needed
    - Returns structured weeks with daily study plans
  - get_current_plan(client, user_id) — fetches active plan from DB
- Constants: _POINTS (per task), _DEFAULT_HOURS (study time estimates), _ROI_ORDER, _REQUIRED_TASKS
- 29 unit tests in tests/test_study_plan_service.py covering:
  - get_required_tasks: 70/80/90/100 target scores (4 tests)
  - _is_mastered: Part1 fast/slow/incorrect, Part2 level_3/2/0 (6 tests)
  - _sort_by_roi: full order, subset, Part2 appended (3 tests)
  - _build_weeks: single task, review allocation, multi-week, empty, spanning (5 tests)
  - generate_plan: target 70/80, mastered skipped, warning, no warning, ROI order, 70/30 split, persistence, weekly structure (9 tests)
  - get_current_plan: found and not found (2 tests)
- All 259 tests passing (29 new), 57 pre-existing failures unchanged, ruff check clean

Next: TASK-016 (Study Plan API endpoints) is now unblocked (depends on TASK-015 done). Also unblocked: TASK-036 (PATCH /api/users/me update).

TASK-016 DONE — Study Plan API: генерация, получение, пересчёт

Implementation:
- Created backend/app/routers/study_plan.py with 4 endpoints:
  - POST /api/study-plan/generate — calls generate_plan(), returns 201 with StudyPlanResponse
  - GET /api/study-plan/current — fetches active plan via get_current_plan(), 404 if none
  - PUT /api/study-plan/recalculate — verifies existing plan exists, then regenerates with new params
  - GET /api/study-plan/today — combines FSRS due cards count + today's study material from plan
    - Counts due fsrs_cards (due <= now, state != new)
    - Finds today's date in plan weeks/days structure
    - Looks up topic titles for each task_number
    - Estimates total minutes (~2min per review card + study + review from plan)
- Registered study_plan router in app/main.py
- All endpoints require authentication via get_current_user dependency
- 13 tests in tests/test_study_plan_api.py covering:
  - POST /generate: creates plan (201), invalid target_score (422), auth required
  - GET /current: returns plan, no plan (404), auth required
  - PUT /recalculate: updates plan, no existing plan (404), auth required
  - GET /today: with plan + due cards, no plan (review only), empty state, auth required
- All 272 tests passing (13 new), 57 pre-existing failures unchanged, ruff check clean

Next: TASK-021 (Study Plan frontend), TASK-022 (Daily checklist on Dashboard), TASK-037 (E2E test) are now unblocked. Also still pending: TASK-036 (PATCH /api/users/me).

TASK-017 DONE — FSRS: автосоздание карточки при решении задачи + обновление attempt

Implementation:
- Updated backend/app/routers/problems.py — replaced _ensure_srs_card with FSRS-based card management:
  - Added _determine_fsrs_rating(is_correct, time_spent_seconds) helper:
    - correct + fast (<60s) → Easy (4)
    - correct → Good (3)
    - incorrect → Again (1)
  - Added _ensure_fsrs_card(client, user_id, problem_id, is_correct, time_spent_seconds):
    - Checks fsrs_cards table for existing card (user_id + problem_id)
    - If no card exists: creates new via fsrs_create_card() + immediately reviews it
    - If card exists: reviews existing card via fsrs_review_card()
  - Removed import of _ensure_srs_card from app.routers.srs
  - Added imports of fsrs_create_card and fsrs_review_card from fsrs_service
- Old SRS code preserved (srs_engine.py, srs router, srs_cards table) — not deleted
- XP continues to be awarded correctly via xp_service
- Streak recording continues via streak_service
- Updated tests/test_problems.py:
  - Fixed pre-existing broken mock chains (maybe_single → execute for users table, data=problem → data=[problem] for problems table)
  - Added patch for _ensure_fsrs_card in attempt tests that focus on XP/correctness logic
  - Added TestDetermineFsrsRating class (5 tests): all rating scenarios + boundary at 60s
  - Added TestEnsureFsrsCard class (4 tests):
    - Creates new FSRS card on first attempt
    - Reviews existing card on repeat attempt
    - srs_cards table not accessed
    - Verify correct args passed to _ensure_fsrs_card from attempt endpoint
- 22 tests in test_problems.py, all passing (9 fixed pre-existing + 9 new)
- All 290 tests passing, 48 pre-existing failures (improved from 57), ruff check clean

Next: TASK-026 (FIRe flow update) and TASK-038 (SM-2 cleanup) are now unblocked by TASK-017.

TASK-006 DONE — Prototype API: CRUD-эндпоинты для прототипов

Implementation:
- Created backend/app/routers/prototypes.py with 4 endpoints:
  - GET /api/prototypes — list all prototypes with optional ?task_number filter, sorted by task_number + order_index
  - GET /api/prototypes/{id} — full prototype detail with theory_markdown, key_formulas, solution_algorithm, common_mistakes, related_prototypes
  - GET /api/prototypes/{id}/videos — video resources linked to prototype, sorted by order_index
  - GET /api/prototypes/{id}/problems — problems linked via prototype_id with pagination (page, page_size), enriched with max_points from topics
- Helper functions: _row_to_list_item, _row_to_detail, _row_to_problem for DB→Pydantic conversion
- All endpoints require authentication (Depends(get_current_user))
- 404 returned for non-existent prototypes on detail/videos/problems endpoints
- Registered prototypes router in app/main.py
- 15 tests in tests/test_prototypes.py covering:
  - List: all prototypes, filter by task_number, empty list, auth required
  - Detail: full fields returned, not found 404, auth required
  - Videos: returns videos with timestamps, empty list, prototype not found, auth required
  - Problems: paginated results with max_points enrichment, empty, prototype not found, auth required
- All 305 tests passing (15 new), 48 pre-existing failures unchanged, ruff check clean

Next: TASK-023 (Prototype detail frontend page) and TASK-026 (FIRe flow with prototypes) are now unblocked by TASK-006. TASK-007/008 (seed prototypes data) are also unblocked and needed to populate the prototypes table.

TASK-019 DONE — Фронтенд: Onboarding flow (целевой балл, часы/день, дата экзамена)

Implementation:
- Created frontend/src/pages/Onboarding.tsx — standalone 3-step onboarding wizard:
  - Step 1: Target score selection — 4 buttons (70/80/90/100) with descriptions
    - 70: "Идеальная Часть 1", 80: "Джентльменский набор", 90: "+ Геометрия и параметры", 100: "Максимум"
    - Selected option highlighted with blue border + bg
  - Step 2: Hours per day — 5 buttons (0.5/1/1.5/2/3 ч/день)
    - Grid layout, responsive on mobile
  - Step 3: Exam date — date picker with default ЕГЭ-2026 (2026-05-28)
    - Shows countdown "До экзамена: N дней"
    - Min date set to today (no past dates)
  - On finish: PATCH /api/users/me with target_score, hours_per_day, exam_date
  - After save: loadUser() + navigate('/diagnostic')
- Step navigation: clickable completed steps (green checkmark), current (blue), pending (gray)
  - Progress connector lines between steps
  - Back/Next buttons with disabled states
  - "Начать диагностику" on final step
- Error handling: displays error message if PATCH fails
- Saving state: button shows "Сохранение..." while request in flight
- Dark mode: full dark: variant support throughout
- Responsive: works on mobile and desktop (max-w-lg centered card)
- Updated App.tsx: added /onboarding as protected route WITHOUT layout (standalone full-screen flow)
- TypeScript clean, frontend builds clean (tsc + vite build), backend ruff + 305 tests pass, 48 pre-existing failures unchanged

Note: hours_per_day is sent in PATCH body but backend UpdateProfileRequest doesn't accept it yet (TASK-036 will add it). The field is silently ignored by the backend for now.

Next: TASK-020 (Diagnostic test UI) and TASK-030 (Routing guard) are closer to being unblocked. TASK-020 depends only on TASK-013 (done). TASK-030 depends on TASK-019 (now done) + TASK-020 + TASK-021.

TASK-020 DONE — Фронтенд: Диагностический тест UI

Implementation:
- Created frontend/src/pages/Diagnostic.tsx — standalone diagnostic test page (no Layout wrapper):
  - On mount: POST /api/diagnostic/start → fetches 19 problems sorted by task_number
  - Progress bar at top showing "Задание X из 19" with percentage fill
  - Per-problem timer that resets on each navigation, displays elapsed time (mm:ss)
  - Recommended time hint per task type (~3-5мин Part 1, ~10-15мин Part 2)
  - Part 1 (tasks 1-12): text input for answer, Enter key support
  - Part 2 (tasks 13-19): 4 self-assessment buttons with color coding:
    - level_0: "Не знаю как подступиться" (red)
    - level_1: "Начал, но не довёл" (orange)
    - level_2: "Решил, не уверен" (yellow)
    - level_3: "Решил уверенно" (green)
  - MathRenderer for LaTeX problem text rendering
  - Task number badge with Part 2 indicator
  - Back/Next navigation between problems (preserves previous answers)
  - Quick navigation dots at bottom showing answered status (green) vs pending (gray)
  - On last problem: "Завершить" button → POST /api/diagnostic/submit with all 19 answers
  - Results screen: summary grid (correct count, Part 1 score, Part 2 count),
    Part 1 grid with green/red per-task results, Part 2 list with self-assessment labels
  - "Перейти к плану" button → navigate to /dashboard
  - Loading spinner, error states, dark mode support throughout
- Updated frontend/src/App.tsx:
  - Added Diagnostic import and /diagnostic route as protected standalone (no Layout)
- TypeScript clean, frontend builds clean (tsc + vite build), backend ruff + 305 tests pass, 48 pre-existing failures unchanged

All acceptance criteria met:
1. /diagnostic loads 19 problems from POST /api/diagnostic/start
2. Progress bar shows current task number and completion percentage
3. Timer ticks per problem, resets on navigation
4. Tasks 1-12: text input field with Enter key support
5. Tasks 13-19: 4 self-assessment buttons (level_0 through level_3)
6. LaTeX rendered via MathRenderer + KaTeX
7. On completion: POST /api/diagnostic/submit with 19 answers, results screen shown

Next: TASK-030 (Routing guard) depends on TASK-019 (done) + TASK-020 (now done) + TASK-021 (pending). TASK-021 (Study Plan page) and TASK-025 (FSRS session UI) are high priority eligible tasks.

TASK-025 DONE — Фронтенд: обновление SRS-сессии под FSRS

Implementation:
- Rewrote frontend/src/stores/sessionStore.ts — switched from SRS to FSRS:
  - New FSRSCard interface with FSRS fields: difficulty (float), stability (float), retrievability (float|null), state, reps, lapses, due, last_review
  - Removed old SRS fields: ease_factor, interval_days, repetition_count
  - Updated ReviewResult interface: added new_due, new_difficulty, new_stability, new_state, retrievability (removed next_review_date, new_interval, new_ease_factor)
  - fetchSession() now calls GET /api/fsrs/session (was /api/srs/session)
  - submitReview() now calls POST /api/fsrs/review with rating (1-4) mapped from assessment strings via ASSESSMENT_TO_RATING lookup
  - Request body sends `rating` integer instead of `self_assessment` string
- Rewrote frontend/src/pages/PracticeSession.tsx — adapted to FSRSCard:
  - fsrsCardToProblem() maps FSRSCard to Problem interface for ProblemCard compatibility
  - Uses card.id instead of card.card_id for identification
  - Shows retrievability badge in header ("X% запоминание") for each card
  - handleComplete builds ReviewResult with FSRS fields from current card state
- Rewrote frontend/src/pages/PracticeResults.tsx — added FSRS analytics:
  - New "FSRS-аналитика" section showing average retrievability (%) and average stability (days)
  - Topic breakdown groups by topic_title (was topic_id) since FSRSCard has no topic_id
  - Stats computed from card.retrievability and review.new_stability
- Practice.tsx unchanged — already uses fetchSession() from store which now calls FSRS endpoint

All acceptance criteria met:
1. PracticeSession uses /api/fsrs/ endpoints (session + review)
2. Cards display retrievability as percentage badge
3. 4 rating buttons (Again/Hard/Good/Easy) map to FSRS Rating 1-4
4. PracticeResults shows FSRS-specific stats (avg retrievability, avg stability)
5. sessionStore fully updated with FSRS types
- TypeScript clean, frontend builds clean (tsc + vite build), backend ruff + 305 tests pass, 48 pre-existing failures unchanged

Next: High priority eligible tasks with all deps met: TASK-021 (Study Plan page, unblocks TASK-030 routing guard), TASK-027 (Score prediction, unblocks TASK-028 + TASK-037), TASK-024 (YouTube player), TASK-031 (RLS security). TASK-034 (SRS card with prototype context) is now unblocked by TASK-025.

TASK-021 DONE — Фронтенд: Страница персонального плана

Implementation:
- Created frontend/src/pages/StudyPlan.tsx — full study plan page with API integration:
  - Loads plan via GET /api/study-plan/current (handles 404 gracefully)
  - Stats summary row: target score, days to exam, hours/day, mastered/total points
  - Warning banner: displays backend warning if insufficient time
  - Mastered tasks: green badges for tasks skipped from plan
  - Weekly timeline: weeks with colored task blocks per day
    - Color-coded by task_number (19 unique colors)
    - Today highlighted with blue background + "СЕГОДНЯ" label
    - Past days shown at reduced opacity
    - Each day shows study blocks (proportional width) + study/review minutes
  - Legend: color key for all task numbers in the plan
  - No-plan state: CTA to start onboarding flow
  - Recalculate modal: target score (4 buttons), hours/day (5 buttons), exam date picker
    - Calls PUT /api/study-plan/recalculate (or POST /generate if no plan exists)
    - Pre-filled with current plan values
- Updated frontend/src/App.tsx: added /plan route with StudyPlan component
- Updated frontend/src/components/Sidebar.tsx: added "Мой план" nav link with calendar icon between Dashboard and Темы
- Dark mode: full dark: variant support throughout
- Loading skeleton, error state, empty state

All acceptance criteria met:
1. /plan displays plan by weeks with colored timeline
2. Timeline visualized with color coding by task_number
3. Mastered points displayed (forecast score proxy)
4. Recalculate button opens modal, calls PUT /api/study-plan/recalculate
5. Warning displayed when user is behind schedule (insufficient time)
6. "Мой план" link added to sidebar
- TypeScript clean, frontend builds clean (tsc + vite build), backend ruff + 305 tests pass, 48 pre-existing failures unchanged

Next: TASK-030 (Routing guard) is now unblocked (deps: TASK-019 done, TASK-020 done, TASK-021 now done). Other high priority eligible: TASK-027 (Score prediction), TASK-024 (YouTube player), TASK-022 (Daily checklist), TASK-031 (RLS security).

TASK-030 DONE — Routing: onboarding guard + new routes

Implementation:
- Updated backend/app/models/auth.py — added has_diagnostic and has_study_plan boolean fields to UserProfile
- Updated backend/app/routers/users.py:
  - Added _check_onboarding_status(client, user_id) helper — queries diagnostic_results and user_study_plan tables
  - GET /api/users/me now returns has_diagnostic and has_study_plan flags
  - PATCH /api/users/me also returns the updated flags
- Created frontend/src/components/OnboardingGuard.tsx — smart redirect guard:
  - No target_score → redirect to /onboarding
  - Has target_score but no diagnostic → redirect to /diagnostic
  - Otherwise → render children (main app)
  - Waits for user data to load before redirecting
- Updated frontend/src/stores/authStore.ts — added has_diagnostic and has_study_plan to User interface
- Updated frontend/src/pages/Diagnostic.tsx:
  - Added loadUser() call before navigating to /dashboard after diagnostic completion
  - Ensures OnboardingGuard sees updated has_diagnostic=true in store
- Updated frontend/src/App.tsx — restructured routing:
  - All protected routes wrapped in ProtectedRoute → OnboardingGuard
  - /onboarding and /diagnostic are standalone (no Layout) but inside guard
  - Main app routes inside Layout, all inside guard
  - Added /prototypes/:id route with placeholder page
- Created frontend/src/pages/PrototypeDetail.tsx — placeholder page for prototype detail (TASK-023 will flesh it out)
- Sidebar already has "Мой план" and "Прогресс" links (from TASK-021)

All acceptance criteria met:
1. New user after login → redirected to /onboarding (no target_score)
2. After onboarding → redirected to /diagnostic (no diagnostic results)
3. After diagnostic → /dashboard accessible (has_diagnostic=true)
4. User with plan → direct access to /dashboard
5. All new routes work: /onboarding, /diagnostic, /plan, /prototypes/:id
6. Sidebar contains links to Plan and Progress
- TypeScript clean, frontend builds clean (tsc + vite build), backend ruff + 305 tests pass, 48 pre-existing failures unchanged

Next: High priority eligible tasks with all deps met: TASK-022 (Daily checklist on Dashboard), TASK-027 (Score prediction), TASK-024 (YouTube player), TASK-031 (RLS security). TASK-023 (Prototype detail page) depends on TASK-007 (seed prototypes) which is still pending.

TASK-022 DONE — Фронтенд: Ежедневный чек-лист на Dashboard

Implementation:
- Updated frontend/src/pages/Dashboard.tsx — added "Сегодня" section between stats row and recommendations
- Fetches GET /api/study-plan/today in parallel with dashboard data (graceful fallback if no plan)
- Added TodayData and DailyTask TypeScript interfaces
- Two-column grid layout:
  1. FSRS Review block: shows due card count with purple theme, "Начать повторение" button → /practice, or "Все карточки повторены!" if 0
  2. New Material block: lists today's study topics from plan with task_number badges, estimated minutes, "Изучить" button → /topics
- Estimated total time shown below the blocks
- No-plan state: indigo CTA card "Создать план" → /onboarding (only shown for non-new users)
- New user welcome banner (pre-existing) still works — no-plan CTA hidden for new users
- Dark mode: full dark: variant support on all new elements
- TypeScript clean, frontend builds clean (tsc + vite build), backend ruff + 305 tests pass, 48 pre-existing failures unchanged

All acceptance criteria met:
1. Dashboard displays "Сегодня" section
2. Review block shows due card count from /api/study-plan/today
3. New material block shows next topics from the plan
4. Buttons link to correct pages (/practice, /topics)
5. Without plan: CTA "Создать план" displayed

Next: High priority eligible tasks with all deps met: TASK-027 (Score prediction, unblocks TASK-028 + TASK-037), TASK-024 (YouTube player), TASK-031 (RLS security). TASK-023 and TASK-033 depend on TASK-007 (seed prototypes).

TASK-027 DONE — Прогноз балла на основе FSRS-состояния карточек

Implementation:
- Added `predict_score(client, user_id, exam_date)` to backend/app/services/study_plan_service.py:
  - Fetches all user's fsrs_cards, maps them to task_numbers via problems and prototypes tables
  - Computes avg_retrievability per task_number using fsrs_service.get_retrievability()
  - Task mastered if avg_retrievability >= 0.8 and cards_count > 0
  - Sums points for mastered tasks using _POINTS dict (Part 1: 1pt, Part 2: 2pt, 18-19: 4pt)
  - Returns predicted_primary_score, predicted_test_score, and per-task breakdown
- Added `_primary_to_test_score(primary)` — linear interpolation score conversion:
  - Conversion table: 0→0, 5→27, 7→40, 12→70, 18→82, 24→94, 32→100
  - Interpolates between control points for any primary score
- Added `_SCORE_CONVERSION` constant for score conversion table
- Added models to backend/app/models/progress.py:
  - TaskScoreBreakdown: cards_count, avg_retrievability, is_mastered, points
  - PredictedScoreResponse: predicted_primary_score, predicted_test_score, breakdown dict
- Added GET /api/progress/predicted-score endpoint to backend/app/routers/progress.py:
  - Reads user's exam_date for retrievability calculation
  - Calls predict_score(), returns PredictedScoreResponse
  - Requires authentication
- 19 unit tests in tests/test_predict_score.py covering:
  - _primary_to_test_score: zero, negative, exact points, interpolation, above max (9 tests)
  - predict_score: no cards (0), mastered Part 1 tasks (7/40), all Part 1 (12/70),
    Part 2 tasks (+points), low retrievability (<0.8), prototype cards, multiple cards averaged,
    boundary 0.8, all 19 tasks in breakdown, points match PRD (10 tests)
- 4 API tests in tests/test_progress.py (TestPredictedScore):
  - Returns predicted score with breakdown, no exam_date, high score with Part 2, auth required
- All 328 tests passing (23 new), 48 pre-existing failures unchanged, ruff check clean

Next: High priority eligible tasks with all deps met: TASK-028 (Dashboard score integration, deps: TASK-022 ✅ + TASK-027 ✅), TASK-024 (YouTube player), TASK-031 (RLS security), TASK-037 (E2E test, deps: TASK-005 ✅ + TASK-014 ✅ + TASK-016 ✅ + TASK-027 ✅). TASK-023 and TASK-033 depend on TASK-007 (seed prototypes).

TASK-028 DONE — Dashboard: интеграция прогноза балла и предупреждений

Implementation:
- Updated frontend/src/pages/Dashboard.tsx — added predicted score widget and retrievability warnings
- Added PredictedScoreData and TaskScoreBreakdown TypeScript interfaces
- Fetches GET /api/progress/predicted-score in parallel with dashboard + today data (graceful catch on error)
- Predicted score widget (between exam countdown and stats row):
  - Large score number (predicted_test_score) with primary score in parentheses
  - Progress bar towards user's target_score (from authStore)
  - Color-coded: green (>= target), yellow (>= 70% of target), orange (below)
  - Percentage label showing progress towards goal
  - "Подробнее" link to /progress page
- Retrievability warnings section (inside score widget):
  - Filters predicted.breakdown for tasks with cards but avg_retrievability < 0.6
  - Shows up to 3 worst tasks sorted by retrievability ascending
  - Each warning shows task number, topic name (from dashboard data), and retrievability %
  - Amber-styled warning box matching existing recommendations design
- Widget hidden when predicted_test_score is 0 (no cards at all)
- Dark mode: full dark: variant support on all new elements
- Imported useAuthStore for target_score access

All acceptance criteria met:
1. Predicted score widget displayed on Dashboard
2. Progress bar shows current vs target score with percentage
3. Warnings displayed for tasks with low retrievability (< 60%)
4. Data loaded from GET /api/progress/predicted-score API
- TypeScript clean, frontend builds clean (tsc + vite build), backend ruff + 328 tests pass, 48 pre-existing failures unchanged

Next: High priority eligible tasks with all deps met: TASK-024 (YouTube player, no deps), TASK-031 (RLS security, deps all met), TASK-029 (Progress page FSRS analytics, deps: TASK-005 ✅). Also eligible: TASK-037 (E2E test), TASK-032 (desired_retention). TASK-023 and TASK-033 depend on TASK-007 (seed prototypes).
