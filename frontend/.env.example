# API URL — local development (default if not set)
# VITE_API_URL=http://localhost:8000

# API URL — production through DuckDNS reverse proxy.
# Keep this as the origin only: frontend code already calls /api/... paths.
# VITE_API_URL=https://repeatify.duckdns.org
